The illustration shows the Create Application dialog box. It contains a form to deploy your application to Oracle Application Container Cloud Service. 

The Application section contains the following fields: 

The Name field contains the name of the application (Sample).

The Metering Frequency list displays the default value (Monthly).

Application Archive has three options (Deploy a Sample Application, Get Archive from Storage Path, and Upload Archive). Upload Archive is selected.

The Choose File button is selected.