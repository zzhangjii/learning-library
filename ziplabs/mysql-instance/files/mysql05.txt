This illustration shows the Details form to create an Oracle MySQL Cloud Service instance. It shows three buttons (Previous, Cancel, and Next), the step (Details), and three sections where you provide the instance service details (Configuration, MySQL Configuration, and Backup and Recovery Configuration). The fields and values identified in the preceding text are shown in the image.